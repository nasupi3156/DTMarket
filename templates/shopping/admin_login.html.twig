<!DOCTYPE html>
  <head>
    <meta http-equiv="content-type" content="text/html; charset=utf-8" />
    
      <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">

     <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <link href="{{constant('shopping\\Bootstrap::APP_URL')}}css/admin_login.css" rel="stylesheet" type="text/css" />
    <link href="{{constant('shopping\\Bootstrap::APP_URL')}}css/admin_header.css" rel="stylesheet" type="text/css" />
    <title>ログイン</title>
  </head>
  <body>
    {% include 'admin_header.html.twig' %}
  
  <div class="container">
    <div class="login_inner">
      <form action="" method="post">
        <div class="login"> 
          <h1>ADMIN</h1>
        </div>
        
        <div class="email">
          <label for="email">メールアドレス</label>
          <input type="email" name="email" value="{{ dataArr.email }}" id="email">
          {# dataArrからメールアドレス(email)の値を取り出し、value属性に設定することで、フォームに戻された際にもユーザーが入力したメールアドレスが表示 #}
          {% if errArr.email %}
            <p style="color: #f00;">{{errArr.email}}</p>
          {% endif %}
        </div>  
        <div class="password">
          <label for="password">パスワード</label>
          <input type="password" name="password" 
          value="{{ password }}" id="password">
          {% if errArr.password %}
          <p style="color: #f00; font-size: 18px;">{{errArr.password}}
          </p>
          {% endif %}
        </div>

          <button type="submit" name="admin" value="ログイン" class="login_btn">ログイン</button>
        </form>
      </div>
      {# contact_inner #}
  </div> 
  {# conteiner #}
  
  </body>
</html>

